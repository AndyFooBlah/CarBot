{
  "name": "carbot-functions",
  "license": "Apache-2.0",
  "description": "CarBot Cloud Functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint src --ext .ts"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/genai": "^1.50.1",
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.3.2",
    "googleapis": "^180.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.8.2"
  },
  "private": true
}
